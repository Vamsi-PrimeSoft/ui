format_version: 10
pipelines:
  UI-CD-YAML:
    group: defaultGroup
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      git-878c2de:
        git: https://github.com/Vamsi-PrimeSoft/ui.git
        username: Vamsi-PrimeSoft
        shallow_clone: false
        auto_update: true
        branch: main
        encrypted_password: AES:CvqRgCac0f8DmOI58hCnLA==:lMJcCvSj3nsKR5i/qjAJUA==
    stages:
      - Deploy-Frontend:
          fetch_materials: true
          keep_artifacts: false
          clean_workspace: false
          approval:
            type: manual
          jobs:
            run-commands:
              timeout: 0
              resources:
               - ubuntu-agent
              variables:
                SFTP_SERVER: 172.16.7.121
                APP_SERVER: 192.168.56.126
                SFTP_USER: ubuntu
                SFTP_KEY_PATH: /var/go/.ssh/id_rsa
                SFTP_DIR: /home/ubuntu
                GOCD_DIR: /home/ubuntu
                APP_SERVER_BUILD_DIR: /var/go
                APP_SERVER_TARGET_DIR: /var/www/html/prime-square
              tasks:
                - exec:
                    command: sudo
                    arguments:
                      - mkdir
                      - -p
                      - ${APP_SERVER_TARGET_DIR}
                - exec:
                    command: sudo
                    arguments:
                      - chown
                      - go:go
                      - ${GOCD_DIR}
                - exec:
                    command: bash
                    arguments:
                      - -c
                      - |
                        sftp -o StrictHostKeyChecking=no -i /var/go/.ssh/id_rsa ubuntu@172.16.7.121 <<EOF
                        get /home/ubuntu/*.zip /var/go
                        bye
                        EOF
                - exec:
                    command: bash
                    arguments:
                      - -c
                      - |
                        sudo chown ubuntu:ubuntu /var/go/*.zip
                        sudo unzip -o /var/go/*.zip -d /var/www/html/prime-square
                        EOF
                - exec:
                    command: echo
                    arguments:
                      - "Frontend deployment complete."
